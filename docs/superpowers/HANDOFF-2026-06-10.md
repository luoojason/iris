# Handoff: job-coordinator branch state (2026-06-10)

Self-contained continuation notes for a fresh session on any machine. The
specs in `docs/superpowers/specs/` are the authority; this file is the map.

## State

Branch `job-coordinator` (do not push to main, merge, or deploy without the
owner). 540 tests green on Python 3.13 and the 3.10 CI floor
(`python -m pytest -q`, ~3s, no network or real claude needed).

Built and committed on this branch:

- Hybrid job coordinator: background `claude -p` jobs via a file-locked
  JobStore registry, Task subagents enabled inside jobs only under the
  IRIS_JOB_GRANTS ceiling, fold-back delivery into the originating
  conversation with notify-spine fallback, cancel/recovery, `iris jobs` CLI,
  five `mcp__jobs__*` tools, all behind the IRIS_JOBS opt-in
  (spec: 2026-06-10-job-coordinator-design.md).
- Credit guard: `iris usage`, `mcp__usage__usage_summary`, 50/80/95%
  threshold pings on the reminders tick, job parking on credit/rate
  pushback, near-cap light-model tightening
  (spec: 2026-06-10-credit-guard-design.md).
- Wiki MCP tools over IRIS_WIKI_DIR with a hardened traversal guard.
- Repo workspaces + artifact delivery: driver cwd, owner-bound name->path
  registry (`iris workspaces add|remove|list`), jobs request workspaces by
  NAME only, `ARTIFACT: <path>` hand-back with traversal/size/count guards
  (spec: 2026-06-10-repo-workspaces-design.md).
- Security: the subagent tool's post-2.1.63 name `Agent` is denied alongside
  `Task`; job grants alias-expand both names.

## Invariants (load-bearing, never break)

Single-user only. Zero idle inference: no model call ever fires on a clock or
poll. Official claude CLI only, through ClaudeDriver/_child_env. The chat
driver's denylist stays untouched. An explicit `disallowed_tools` REPLACES
the default denylist, so job denylists always derive from DANGEROUS_BUILTINS.
The model never names paths or delivery channels: only owner-bound workspace
names and recorded ids.

## Conventions

Strict TDD (failing test first). No conftest.py; fakes are local per test
file. No real claude/network/sleeps in tests; injected clocks and seams.
Python 3.10-compatible syntax. Plain commit messages, no marketing, no
emojis. Commit per completed unit with the full suite green.

## Do in order

1. Workspaces docs gap: README's "Background jobs" section and .env.example
   are missing the workspaces + artifacts material (IRIS_WORKSPACES_FILE,
   the names-not-paths security model, the CLI, the `ARTIFACT:` report
   convention, the 5-file/8MB caps). Small commit.
2. Event wakes core: spec 2026-06-10-event-wakes-design.md. The
   implementation does not exist; a red-phase draft test from a crashed
   agent is parked at `tests/test_wakes.py.red`. Rename it back to `.py`,
   review it against the spec (it is draft, not authority), build
   `iris/wakes.py` per the spec, then the wiring commit: IRIS_WAKES_FILE /
   IRIS_WAKES_STATE config knobs, a tick_wakes call in reminders-tick beside
   budget_tick, doctor validation of the rules file, README/.env.example.
3. Adversarial review pass over everything since the workspaces spec commit
   (concurrency, the invariants above, silent failures, test quality); fix
   confirmed findings; full suite green on 3.10 and current.

## Blocked on the owner (do not start)

The Job console build (owner answers pending on the seven questions in
2026-06-10-job-console-design.md), pushing to main / merging, deploying to
the production box, and the live Discord shakedown.
